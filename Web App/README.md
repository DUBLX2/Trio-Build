Trio-Build React scaffold

This folder contains a minimal React application scaffold. Install dependencies and run from the `Trio-Build` folder.

Install and run:

```bash
cd Trio-Build
npm install
npm start
```

Notes:
- This scaffold uses `react-scripts` (Create React App) for simplicity.
- If you prefer Vite or TypeScript, I can re-scaffold accordingly.

import React from 'react';

export default function Home() {
  return (
    <section>
      <h2>Welcome</h2>
      <p>This is a minimal React scaffold inside the <strong>Trio-Build</strong> folder.</p>
      <p>Run <code>npm install</code> then <code>npm start</code> to launch the dev server.</p>
    </section>
  );
}
